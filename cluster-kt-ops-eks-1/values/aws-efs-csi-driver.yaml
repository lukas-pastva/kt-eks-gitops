controller:
  serviceAccount:
    name: efs-csi-controller-sa
    annotations:
      eks.amazonaws.com/role-arn: "" # TODO: Set EFS CSI driver IAM role ARN

storageClasses:
  - name: efs-sc
    parameters:
      provisioningMode: efs-ap
      fileSystemId: " fs-0fe57b05230e1f313"
      directoryPerms: "700"
    reclaimPolicy: Delete
    volumeBindingMode: Immediate
